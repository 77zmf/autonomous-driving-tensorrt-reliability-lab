# Local artifacts

Raw TensorRT engines, build logs, profiler captures, and large intermediate files stay local and are ignored by git.

Publish only small, reviewed derived summaries that include the environment manifest and reproduction command. Raw artifacts may contain paths, hostnames, model details, or other sensitive information and must be inspected before sharing.
