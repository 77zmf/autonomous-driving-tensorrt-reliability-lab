# Local models

Model binaries are ignored by git. Use only public models whose licenses permit this work.

For every local model, record its source URL, license, exact revision, file SHA-256, export command, ONNX opset, input/output contract, and preprocessing/postprocessing revision in an experiment manifest.

Never place employer or customer models in this repository.
